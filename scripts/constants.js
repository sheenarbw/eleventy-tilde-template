const TOPIC = "topic";
const PROJECT = "project";
const NONE = "none";
const COURSE = "course";
const ALLOWED_CONTENT_TYPES = [NONE, COURSE, PROJECT, TOPIC];

module.exports = {
  NONE,
  COURSE,
  PROJECT,
  TOPIC,
  ALLOWED_CONTENT_TYPES,
};
